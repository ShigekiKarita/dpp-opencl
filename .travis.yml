dist: bionic

language: d

d:
  - ldc
  # - dmd

# addons:
#   apt:
#     packages:
#       - clinfo
#       - libpocl-dev
#       - libclang-6.0-dev

cache: apt

script:
  - sudo apt-get update -y
  - sudo apt-get install -y clinfo libpocl-dev libclang-6.0-dev
  - clinfo
  - dub run
